{% extends 'base.html.twig' %}

{% block title %}PostTema{% endblock %}

{% block body %}
    <h1>PostTema</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_tema.id }}</td>
            </tr>
            <tr>
                <th>Texto</th>
                <td>{{ post_tema.texto }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ post_tema.fecha ? post_tema.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_index') }}">back to list</a>

    <a href="{{ path('post_edit', {'id': post_tema.id}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
